<% @admissions.each do |admission| %>
  <li class="list-group-item" role="option" data-autocomplete-value="<%= admission.id %>" data-autocomplete-label="<%= admission.name %>">
    <%= admission.name %> (<i><%= admission.created_at %></i>)
  </li>
<% end %>
